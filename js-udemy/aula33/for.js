//Estruturas de repetição (for)


// dentro do (), primeiro criar variavel e indicar o inicio, segundo condição (onde parar), terceiro  incrementação (a cada volta do laço)
for (let i = 0; i <= 5; i ++ ) {
    console.log(`Linha ${i}`);
} //Nao usar ; no final

